export enum UserRole {
  USER = "USER",
  ADMIN = "ADMIN",
  CLIENT = "CLIENT",
}

export enum StockType {
  STOCK = "STOCK",
  PRE_STOCK = "PRE-STOCK",
  LIVE = "LIVE",
}

export enum ORDER_STATUS {
  PAID = "PAID",
  DONE = "DONE",
  PENDING = "PENDING",
  RETURN = "RETURN",
}
